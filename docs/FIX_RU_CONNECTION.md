# Быстрое решение проблемы подключения VPN в РФ

## Симптомы
- ✅ VPN подключен (handshake активен)
- ❌ Интернет не работает
- ❌ Сайты не открываются

## Решение

### Вариант 1: Пересоздание конфигурации (Рекомендуется)

Если пользователь использует старую конфигурацию с неправильным DNS:

1. **Найти USER_ID пользователя:**
   ```bash
   cd /opt/vpn-service/scripts
   bash get-user-id.sh TELEGRAM_ID
   ```

2. **Пересоздать конфигурацию:**
   ```bash
   bash recreate-user-config.sh USER_ID
   ```

3. **Пользователю нужно:**
   - Удалить старую конфигурацию из WireGuard приложения
   - Получить новую через бота (`/devices` → создать новое устройство)
   - Импортировать и подключиться

### Вариант 2: Через админку

1. Зайти в админку
2. Найти пользователя
3. Нажать "Сбросить trial" (это удалит все peer'ы и позволит создать новый)
4. Пользователю создать новое устройство через бота

### Вариант 3: Проверка DNS на сервере

1. **Проверить DNS в базе данных:**
   ```bash
   sudo -u postgres psql -d vpn_service -c "SELECT name, dns FROM vpn_servers;"
   ```

2. **Обновить DNS если нужно:**
   ```bash
   cd /opt/vpn-service/scripts
   bash update-server-dns.sh http://localhost:3000
   ```

3. **Рекомендуемый DNS для РФ:**
   - `1.1.1.1,1.0.0.1,8.8.8.8,8.8.4.4` (Cloudflare + Google)

### Вариант 4: Диагностика через скрипт

```bash
cd /opt/vpn-service/scripts

# Диагностика конкретного пользователя
bash diagnose-vpn-ru-detailed.sh http://localhost:3000 USER_ID

# Или по Telegram ID
bash diagnose-vpn-ru-detailed.sh http://localhost:3000 TELEGRAM_ID
```

## Проверка на устройстве пользователя

Попросите пользователя проверить:

1. **IP адрес:**
   - Откройте любой браузер
   - Перейдите на https://ifconfig.me
   - Должен показать IP VPN сервера, а не реальный IP

2. **DNS резолвинг:**
   - На Android: использовать приложение типа "DNS Lookup"
   - Проверить что `google.com` резолвится

3. **Конфигурация WireGuard:**
   - Открыть WireGuard приложение
   - Нажать на конфигурацию → "Посмотреть/Экспортировать конфигурацию"
   - Проверить что есть строка: `DNS = 1.1.1.1,1.0.0.1,8.8.8.8,8.8.4.4`
   - Проверить что `AllowedIPs = 0.0.0.0/0,::/0`

## Быстрое решение через админку

1. Зайти в админку: `http://SERVER_IP:3000/admin?token=YOUR_TOKEN`
2. Перейти на вкладку "Пользователи"
3. Найти пользователя
4. Нажать "🔄" (миграция) → выбрать другой сервер (если доступен)
   - ИЛИ нажать "✕" (деактивация peer) → пользователю создать новое устройство

## Если ничего не помогает

1. **Изменить порт WireGuard** (если блокируется):
   - В админке: Серверы → "Порт" → изменить на 443 или другой
   - Обновить конфигурацию WireGuard на сервере
   - Пользователю получить новую конфигурацию

2. **Проверить что IP сервера не заблокирован в РФ:**
   - Попросить пользователя проверить доступность: `ping SERVER_IP`
   - Если не пингуется - нужно мигрировать на другой сервер

3. **Проверить логи WireGuard:**
   ```bash
   sudo journalctl -u wg-quick@wg0 -n 100 | grep -i error
   ```

## Частые проблемы

### Проблема: DNS заблокирован в РФ
**Решение:** Использовать `1.1.1.1,1.0.0.1` (Cloudflare) или настроить собственный DNS сервер на VPN сервере

### Проблема: Пользователь использует старую конфигурацию
**Решение:** Пересоздать конфигурацию (см. Вариант 1)

### Проблема: Порт WireGuard блокируется
**Решение:** Изменить порт на 443/UDP через админку

### Проблема: IP сервера заблокирован
**Решение:** Мигрировать пользователя на другой сервер через админку

